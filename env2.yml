name: new
channels:
  - conda-forge
  - defaults
dependencies:
  - libsndfile
  - pip
  - python=3.11.2
  - scipy
  - pip:
    - librosa
    - numba
    - numpy
    - pandas
    - pyyaml
    - scikit-learn
    - tqdm
    - torch
